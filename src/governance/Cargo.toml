[package]
name = "governance"
version = "0.0.0"
edition = "2021"

[dependencies]
soroban-sdk = "22.0.0"

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }

[lib]
name = "governance"
path = "lib.rs"
crate-type = ["cdylib", "rlib"]
